# Server Port (Railway automatically sets PORT, defaults to 3000 if local)
PORT=3000
NODE_ENV=production

# Gemini AI API Key for Automated Tax & Corporate Formation Advisor
GEMINI_API_KEY=

# Allowed Frontend URL for CORS (Optional: defaults to allow all origins if left blank)
FRONTEND_URL=https://uomamabusiness.com
